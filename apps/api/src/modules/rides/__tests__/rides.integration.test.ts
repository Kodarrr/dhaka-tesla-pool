// Integration tests require a live database connection.
// Run with: DATABASE_URL=<url> npm test
import { describe, it } from 'vitest';

describe.skip('rides integration (requires database)', () => {
  it('placeholder — see manual test checklist', () => {});
});
